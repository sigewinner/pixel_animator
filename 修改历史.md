# 修改历史

> 本文件记录本项目所有文件变更。每次修改请在本文件**最末尾**追加一条记录，写明：被更改的文件、更改日期、修改大致内容。
> 配套规则见 `WorkBuddy运作前必读.md`。

## 2026-07-22 初始化：创建修改历史与必读规则
- 更改文件：`修改历史.md`（新建）、`WorkBuddy运作前必读.md`（新建）
- 大致内容：在工作文件夹下新建「修改历史」文件（本文件）与「WorkBuddy 运作前必读」文件，约定两条规则——(1) 改动任意文件后须校对父级文件夹中的 `PixelAnimator_软件架构与功能实现说明书.md`，不对应则同步更新；(2) 每次修改都须在本文件末尾追加记录（被改文件 / 日期 / 大致内容）。

## 2026-07-23 帧删除限制放开 + 批量删除修复 + 帧预览刷新 + 说明书迁移
- 更改文件：
  - `pixel-animator/public/js/animation.js`（`deleteFrame` 放开"至少保留一帧"、删空加载空白帧；`getThumbnail` 空工程返回空白缩略图）
  - `pixel-animator/public/js/modules/frames.js`（`btnDelFrame` 移除守卫，直接调用 `deleteFrame`）
  - `pixel-animator/public/js/modules/batch.js`（`deleteSelected` 移除"至少保留一帧"拦截，降序 splice 实现"选中几张删几张"，支持删到空工程）
  - `pixel-animator/public/js/app.js`（音效包装层 `deleteFrame` 改 `frames.length >= 1`；`onDrawEnd` 补 `renderFrameList()` 使帧列表预览每次绘制后刷新）
  - `PixelAnimator_软件架构与功能实现说明书.md`（重建并内嵌本次修订；因父级文件夹写入在本环境不跨会话持久化，已从父级迁至**工作文件夹**同目录）
  - `WorkBuddy运作前必读.md`、`MEMORY.md`（说明书路径改指向工作文件夹）
- 大致内容：
  1. 放开单帧/批量删除的"至少保留一帧"限制，可删至空工程且安全处理（空工程加载空白帧、空白缩略图）。
  2. 修复批量删除只删一张的问题（降序 splice 防索引错位，选中几张删几张）。
  3. 帧列表缩略图预览在每次操作（含绘制完成）后刷新，不再仅新增帧时刷新。
  4. 因本环境对父级文件夹写入不持久，说明书从父级迁移到工作文件夹，并同步更新必读规则与 MEMORY 的路径指向。

## 2026-07-23 快捷键补全+复制粘贴、网格棋盘格、撤销全局化、必读新增规则
- 更改文件：
  - `pixel-animator/public/index.html`（重写全局 `keydown`：修复 `c`→`shape`（原映射 `circle` 无对应按钮导致失效）；新增 Ctrl+C 复制、Ctrl+V 粘贴；新增 Shift+Ctrl+Z 重做（保留 Ctrl+Y）；修饰键下不再误触工具键；输入框/文本域中不透传；重做按钮 tooltip 更新）
  - `pixel-animator/public/js/app.js`
    - 修正撤销语义：`undoOperation` 改为弹出栈顶（当前操作）移入重做栈、再恢复栈中倒数第二状态（操作前）；`redoOperation` 恢复重做栈顶；`init()` 末尾种入初始快照作为撤销"地基"。**此前各操作在变更后才 `pushSnapshot`，旧算法弹栈顶即当前状态=撤销无效，本次修复后绘制/增删帧/翻转旋转/图层/清空等原有快照真正可撤销。**
    - 快照新增 `penSize`/`eraserSize`；`restoreSnapshot` 还原笔大小与滑块 UI。
    - 新增复制粘贴：`copySelection`/`pasteSelection`/`pasteFrame`/`pasteLayer`，剪贴板存于 `PA.state.frameClipboard`/`layerClipboard`；Ctrl+C 复制当前帧+当前图层，Ctrl+V 按图层面板是否打开分流粘贴（图层/帧），均经 `pushSnapshot` 可撤销。
  - `pixel-animator/public/js/modules/toolbar.js`（笔/橡皮大小滑块：拖动 `input` 实时更新，松开 `change` 时 `pushSnapshot`，使笔大小变化可撤销）
  - `pixel-animator/public/js/canvas-engine.js`（`render()` 背景改为由 `showGrid` 决定：激活=灰白棋盘格 `#ffffff`/`#d9d9d9`，关闭=纯白；`showGrid` 默认 `true` 已满足默认激活）
  - `WorkBuddy运作前必读.md`（新增规则 3：新功能必须能被撤销，须调用 `PA.pushSnapshot()`，必要状态在 `saveSnapshot/restoreSnapshot` 中保存还原）
  - `PixelAnimator_软件架构与功能实现说明书.md`（同步更新：5.4 快照语义、7.3 网格棋盘格背景、7.12 撤销+复制粘贴、模块地图 app.js/toolbar.js/canvas-engine.js/index.html 行号与职责、UI 映射快捷键说明；功能清单补充复制粘贴与全局撤销）
- 大致内容：
  1. 快捷键：补齐并修复——`c` 键修复为切换形状工具；新增 Ctrl+C/V 复制粘贴（作用于图层与帧，按上下文分流）；新增 Shift+Ctrl+Z 重做（Ctrl+Y 兼容）；修正 Ctrl 组合误触工具键。
  2. 网格：激活时画布背景为灰白棋盘格、关闭为纯白，默认激活（按钮已带 active）。
  3. 撤销全局化：修正撤销算法使其真正生效，并扩展到笔大小变化（滑块 change 时记录）；绘制/增删帧/翻转旋转/图层/清空/复制粘贴等均已可撤销。
  4. 必读规则新增第 3 条：新增功能须接入撤销（调用 `PA.pushSnapshot()` 并在快照中保存/还原所需状态）。

## 2026-07-24 分辨率新增 256/512/1080 选项 + 自定义分辨率功能
- 更改文件：
  - `pixel-animator/public/index.html`（`resolutionSelect` 新增 256/512/1080 px 选项与「自定义」选项；新增 `#customResolution` 数字输入框，选中自定义时显示；app.js/canvas-size.js 缓存版本号 v8→v9）
  - `pixel-animator/public/js/modules/canvas-size.js`（新增 `getResolution()` 统一读取分辨率值——自定义模式时从输入框解析并 clamp 2–1080；`bindCanvasSize` 监听 select change 切换输入框显隐与 input 事件实时预览；`updateSizePreview`/`applyCanvasSize` 改用 `getResolution()`）
  - `pixel-animator/public/js/app.js`（`computePixelSize` 最小像素从 4 改为 1，使大分辨率画布不超出显示区；`syncSizeSelectors` 预设列表新增 256/512/1080，不匹配时切到自定义模式并回填值；`init()` 改用 `PA.CanvasSize.getResolution()`）
  - `PixelAnimator_软件架构与功能实现说明书.md`（7.19 节补充新分辨率预设、自定义分辨率功能、`computePixelSize` 最小值变更说明）
- 大致内容：
  1. 分辨率下拉框新增 256/512/1080 px 三个预设选项。
  2. 新增「自定义」分辨率：选中后显示数字输入框（范围 2–1080），输入后实时预览尺寸，点应用生效。
  3. `computePixelSize` 最小像素从 4 降为 1，使 256/512/1080 等大分辨率画布在显示区域内合理缩放（原有小分辨率不受影响）。

## 2026-07-24 性能根治：ImageData 渲染 + 冗余历史移除 + 快照 COW + RLE 存储 + 缩略图降采样
- 更改文件：
  - `pixel-animator/public/js/canvas-engine.js`（`render` 重写：离屏小画布 + ImageData + drawImage 最近邻放大替代逐像素 fillRect；新增 `_ensureOffscreen`/`_getRGBA`(颜色缓存)/`_drawOnionLayer`；移除 `_onDown`(416) 与 `clear`(1076) 的 `pushHistory()` 冗余深拷贝）
  - `pixel-animator/public/js/app.js`（新增 RLE 编解码 `rleEncode/rleDecode/encodeFrames/decodeFramesPayload/encodeLayerFramePixelData/decodeLayerFramePixelData/encodeLayerSnapshot/decodeLayerSnapshot` 并挂 `PA`；`getCurrentProjectData` 编码 frames+layerData；`loadProject` 解码；`saveSnapshot` 帧外层 `.slice()` 共享内层引用（COW），`restoreSnapshot` 仍深拷贝；`MAX_HISTORY` 100→50）
  - `pixel-animator/public/js/animation.js`（`getThumbnail` 最长边封顶 256px：1:1 渲染后整体缩放，移除 ps=4 的 4320px PNG 炸弹）
  - `pixel-animator/public/js/modules/export.js`（`saveWork`/`saveToLocalFile` 编码 frames+layerData；`loadFromLocalFile` 解码，兼容旧格式）
  - `pixel-animator/public/js/gallery.js`（本地 RLE 解码 `rleDecode/decodeFramesPayload`，预览前解码 frames，兼容新旧格式）
  - `pixel-animator/public/index.html`、`pixel-animator/public/gallery.html`（改动 JS 缓存版本号 v9→v10 / gallery.js 加 ?v=10）
  - `PixelAnimator_软件架构与功能实现说明书.md`（新增 7.23 性能优化章节）
- 大致内容：
  1. 渲染根治：1080px 下从 116 万次 fillRect 改为单次 ImageData+drawImage，消除实时卡顿。
  2. 内存：移除引擎自带冗余历史深拷贝（1080px 约省 1.5GB）；撤销快照改为帧级 COW，栈内存从 100×全帧降到约 100×单帧；MAX_HISTORY 减半。
  3. 存储：RLE 行程编码，1080px 空白帧 JSON 约 5.5MB → 约 100 字节；.pixa/草稿/作品库全链路编码，旧格式自动兼容。
  4. 缩略图最长边封顶 256px，移除 4320px 体积炸弹。
